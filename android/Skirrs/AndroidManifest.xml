<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.skirrs"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/SkirrsTheme" >

        <!-- Main activity is Login -->
        <activity
            android:name="com.skirrs.SignInActivity"
            android:label="@string/app_name"  >
            
            <!-- Setting noHistory to true will prevent users from pressing the back button and
                    ending up on a sing-in screen after the user has signed-in -->
            <!-- android:noHistory="true" android:excludeFromRecents="true" -->
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            android:windowSoftInputMode="adjustResize|stateVisible" >
        </activity>

        <!-- Register activity -->
        <activity
            android:name="com.skirrs.RegisterActivity"
            android:label="@string/com.skirrs.title_activity_register" >
        </activity>
        <activity
            android:name="com.skirrs.HomeActivity"
            android:label="@string/title_activity_home" >
            <!-- <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter> -->
        </activity>
    </application>

</manifest>
